'use strict'

const { User } = require('./users');
const { Restaurant } = require('./restaurants');
const { MenuItem } = require('./menuItems');

module.exports = {
   User,
   Restaurant,
   MenuItem
}